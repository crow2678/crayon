## What

<!-- What does this PR change? -->

## Why

<!-- Why does it need to change? Link issues if relevant. -->

## Checklist

- [ ] Tests added or updated
- [ ] `npm run check:deps` passes (no new runtime dependencies)
- [ ] `npm run typecheck` passes
- [ ] `npm run build && npm run size` passes the size budget
- [ ] If perf-sensitive: bench numbers included in the PR description
